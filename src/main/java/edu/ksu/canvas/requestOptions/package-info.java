/**
 * Classes to encapsulate parameters that get passed to API calls.
 * <p>
 * Some API calls take a huge list of (often optional) parameters. The
 * classes in this package allow type-safe usage of these parameters
 * and allow new parameters to be added without breaking existing code.
 */
package edu.ksu.canvas.requestOptions;